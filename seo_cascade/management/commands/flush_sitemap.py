from django.core.management.base import BaseCommand
from ...middleware import generate_sitemap


class Command(BaseCommand):
	args = 'None'
	help = "Update the sitemap generated by seo_cascade. Ping google with changes."

	def handle(self, *args, **kwargs):
		generate_sitemap()
